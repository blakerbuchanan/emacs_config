:DOC-CONFIG:
# Tangle by default to config.el, which is the most common case
#+property: header-args:emacs-lisp :tangle config.el
#+startup: fold
:END:

#+title: Doom Emacs Configuration
#+author: Blake Buchanan
#+email: blakerbuchanan@gmail.com

* Table of Contents :TOC_3:noexport:
- [[#references][References]]
- [[#overview][Overview]]
- [[#what-is-in-my-initel-file][What is in my init.el file?]]
- [[#what-is-in-my-configel-file][What is in my config.el file?]]
- [[#what-is-in-my-packagesel-file][What is in my packages.el file?]]

* References
This org file is based on Diego Zamboni's, [[https://github.com/zzamboni/dot-doom/blob/master/doom.org][found here]]. [[https://github.com/emacs-tw/awesome-emacs?tab=readme-ov-file#ai][Awesome Emacs]] also contains a comprehensive list of useful Emacs packages.

* Overview
This org file was created as a means of having a persisting Doom emacs configuration across devices / needed reconfigurations.

* What is in my init.el file?

The header for this file as it is installed by Doom Emacs looks like:

#+html: <details><summary>init.el</summary>
#+begin_src emacs-lisp :tangle init.el
;;; init.el -*- lexical-binding: t; -*-

;; DO NOT EDIT THIS FILE DIRECTLY
;; This is a file generated from a literate programing source file located at
;; https://gitlab.com/zzamboni/dot-doom/-/blob/master/doom.org
;; You should make any changes there and regenerate it from Emacs org-mode
;; using org-babel-tangle (C-c C-v t)

;; This file controls what Doom modules are enabled and what order they load
;; in. Remember to run 'doom sync' after modifying it!

;; NOTE Press 'SPC h d h' (or 'C-h d h' for non-vim users) to access Doom's
;;      documentation. There you'll find a "Module Index" link where you'll find
;;      a comprehensive list of Doom's modules and what flags they support.

;; NOTE Move your cursor over a module's name (or its flags) and press 'K' (or
;;      'C-c c k' for non-vim users) to view its documentation. This works on
;;      flags as well (those symbols that start with a plus).
;;
;;      Alternatively, press 'gd' (or 'C-c c d') on a module to browse its
;;      directory (for easy access to its source code).
#+end_src
#+html: </details>



#+begin_src emacs-lisp :tangle init.el
;; here is where all of the init.el goes for Doom. tangle writes it to the init.el file

#+end_src

* What is in my config.el file?
#+begin_src emacs-lisp
;; here is where we want to put all config.el customizations for doom
#+end_src

* What is in my packages.el file?

#+begin_src emacs-lisp :tangle packages.el

#+end_src
